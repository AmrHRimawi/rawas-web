/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    // basePath: "/rawas-web",
    // assetPrefix: "/rawas-web/",
    images: {
        unoptimized: true,
    }
};

export default nextConfig;
